# colours-of-cities
The site works from a database that was created by analysing images of different landmarks of a given city. That data is then loaded into the site and is displayed as an organised palette along with the city name. You can not only see the current colour hex code that you're hovering above, but also copy the code directly onto your clipboard by simply clicking on the colour.
